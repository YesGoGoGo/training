-- Find the titles of all movies that have no ratings. 

-- select * from
--    (
--     select m.title t, r.stars s
--     from movie m, rating r
--    ) as tr

-- Maybe need to create two queries, and then figure
-- out where there is no intersection between mid?
